改动记录

2026-09-15
- 重启后概率掉圣遗物/武器：
  - 根因：登录 `Inventory.loadFromDatabase` 装不上就把 `equipCharacter` 清 0 写库；`equipItem` 把「自己正在穿」当别人卸。
  - 处理：失败只 warn 不落库卸装；`equipItem` 忽略 self。
  - 注入：`_deploy_equip_restore_remote.py`（javassist 改 Avatar+Inventory，备份 bak-before-equip-restore-）。已重启。请重登验证。
- 陆地水圈 vs 水下战技不必二选一：
  - 根因：Absorb 等进 DEFAULT 后，任意角色登录/切队/死亡/复活重建 embryo 都会播水圈（官方 ConfigGlobalCombat 也不把它们放默认）。
  - 处理：Absorb/Dive_Team 全部移出默认；潜水时动态挂上，仅明确陆地动作才卸；瞄准/FIGHT 期间保持；另拦 CreateGadget(40034001)。
  - 注入：_deploy_dive_both_remote.py（GameConstants+DiveAbilityHelper+ActionCreateGadget，备份 bak-before-dive-both-）。请重登：陆地切队无圈；下水后可长按瞄准/重复吸收技。
- 水下长按瞄准/吸收技只能放一次（误伤）：
  - 根因：上一轮为去陆地涟漪把 Absorb 从默认清掉，且 onMotionChange「非潜水即卸」——瞄准/开战技会离开 MOTION_DIVE_*，能力被拆掉。
  - 处理：Absorb/SealEcho/CrabShield/DiveStamina 回 DEFAULT；仅 Dive_Team 动态挂载；只在明确陆地动作 forceClean。
  - 注入：_deploy_dive_unbreak_remote.py（仅 GameConstants+DiveAbilityHelper，备份 bak-before-dive-unbreak-）。请重登验证：陆地无涟漪 + 水下可长按瞄准 + 吸收技可重复。
- 远程纳塔燃素条又不显示：
  - 排查：全服 ENABLE=1 但 MAX/CUR 恒为 0；DynamicAbility_Phlogiston 仅在 getScene()==101 时下发，进图易被跳过。
  - 首轮整类替换无效。已回滚 EnterScene/Handler/Avatar/AbilityManager，最小注入：仅 Phlogiston.ensurePlayerProps（登录补 max/cur）+ TeamManager 始终挂 DynamicAbility_Phlogiston。
  - 备份 bak-before-phlogiston-min-；请重登验证。

2026-09-14
- 陆地水圈仍在（切队/死亡/复活/进游戏）：
  - 根因：Absorb_Shoot 等仍在默认 embryo；Dive_Team 可能残留在 teamAbilityEmbryos，能力重建即播涟漪。
  - 处理：默认列表清空全部潜水/吸收能力；DiveAbilityHelper 仅潜水动作挂载，切队/进场景 forceClean；TeamManager 陆地不下发 Dive_Team。
  - 注入：_deploy_dive_land_clean_remote.py（备份 bak-before-dive-land-clean-）。水下攻击靠动态挂载恢复。
- 水下攻击 CD 显示巨型数字（如 1787614000）：
  - 根因：AllShareCDDataNotify 的 _cd_end_time 误用墙钟毫秒；进水时又批量推送“就绪”包。
  - 处理：仅改 ShareCDHelper（改用 Scene.getSceneTime）+ DiveAbilityHelper（挂载时不再推 ShareCD）。不碰其它文件。
  - 注入：_deploy_sharecd_fix_only.py（备份 bak-before-sharecd-fix-）。
- 陆地切队又出水波纹圈（与水下攻击并存）：
  - 根因：Ability_Avatar_Dive_Team 常驻默认 embryo，切队/登录重建时 CreateGadget(40034001) 播蓝色涟漪。
  - 处理：从 DEFAULT_ABILITY / DEFAULT_TEAM 去掉 Dive_Team；保留 Absorb 战技 embryo；新增 DiveAbilityHelper，仅在 MOTION_DIVE_* 时动态挂到队伍，上岸卸下。
  - 注入：_deploy_dive_dynamic_remote.py（GameConstants + DiveAbilityHelper + HandlerCombatInvocationsNotify）。
- 枫丹水下攻击键 CD 卡在 1.0 不转：
  - 根因：源水普攻 (skill 20046) 走 AvatarShareCDSkillStart + ShareCDExcel（coolDownTime=1），服务端无 ShareCD 处理/不下发 AllShareCDDataNotify。
  - 处理：新增 ShareCDHelper、PacketAllShareCDDataNotify、ActionAvatarShareCDSkillStart；补 Add/RemoveAvatarSkillInfo 同步充能。
  - 注入：_deploy_sharecd_remote.py → 远程（备份 bak-before-sharecd-）。重登下水后 CD 应按 1s 走完。
- 枫丹水下无攻击键（能游不能打）：
  - 根因：9/8 为修陆地水圈，把 Ability_Avatar_Dive_Team 及 Dive Absorb 一并从默认 embryo 去掉（远程 jar 还在 clinit 末尾过滤整批潜水能力）。
  - 处理：恢复水下战技/异色生物吸收相关 embryo；继续排除 Avatar_FluidAgitator、SceneAbility_DiveVolume（后者由场景 levelElementAbilities 提供）。
  - 注入：_deploy_dive_attack_remote.py → 远程 LunaGC-src-run.jar（备份 bak-before-dive-attack-），重启后请重登下水验证左键攻击键。
- 冰元素主角附魔普攻无伤害/无受击：
  - 根因：StarSuperconducted_Attack* 仅在 SkillUpgrade AddAbility 中声明，未进 AbilityControlBlock。
  - 处理：IceTravelerAbilityHelper 下发时补 embryos；ByUnlockTalentParam 按 TalentParamEntry 判定；Ability 应用 inherent proud 参数；资源换用 es.7z 冰 Ability/SkillObj。
  - 注入：_build_inject_ice_embryo.py / _deploy_ice_embryo_remote.py；资源 gitignore，部署时单独同步。
- 温迪风附魔 0 伤 + 身下多出一个大招（Downloads\温迪.rar）：
  - 开大/附魔普攻时服务端对客户端 SkillObj（41022001 风眼、41022002/03/14 风刃）再建空壳，落在脚下像第二个大招或 E。
  - 魔导附魔箭 `Hexenzirkel_NormalAttack_Ratio` 默认 0，未套天赋倍率。
  - 处理：新增 `VentiSkillObjHelper` 跳过并清理空壳；战斗包补算附魔箭伤害；AbilityManager 给客户端 gadget 套 proud-skill 倍率。
  - 已打入本地 `LunaGC-7.0.0.jar`（备份 `bak-before-venti-ability-20260914204713`）并重启。远程已注入 `/www/wwwroot/LunaGC-src-run.jar`（备份前缀 `bak-before-venti-`）并重启。请重登后用温迪验证。
  - 续：远程附魔普攻有伤无飘字——客户端仍用 Ratio=0 自算，服务端只补了扣血。现对客户端 gadget 推送 `OVERRIDE_PARAM`（`VentiSkillObjHelper.pushTalentOverrides` / EvtCreate 后再 sync），战斗回写补 Anemo elementType。
  - 续：全量 ClientGadget OVERRIDE 推送导致进场景白屏。已改为仅 Hexenzirkel/温迪附魔箭能力同步，并加 try/catch；远程已重启。
  - 续：飘字仍无——按箭 gadget json（`Venti_HexenzirkelArrow*`）同步 OVERRIDE，补伤后下发 `EvtBeingHitNotify`。远程/本地已注入。
- DPS 测试内置（原 MeChen618/DPS-Plugin，不再需要 plugins/ 下的 jar）：
  - 新增 `emu.grasscutter.game.dps`：`DPSMeter`（入口）、`DPSChallenge`（挑战+结算）、`DPSEntity`（靶子）、`DPSTimeTrigger`（计时）。
  - 免 `/` 前缀：聊天框直接发 `dps30秒` 开测（也认 `dps30`/`dps30s`、全角数字），`dps停止`/`dps结束` 提前收尾；上限 120 秒，缺省 60 秒。
  - 兼容 `/dps [<秒数>] [x<靶子数量>]` 与 `/dps stop`；命令走 `@Command` 自动扫描注册，`ChatSystem.tryInvokeCommand` 负责免前缀那条路。
  - 靶子为丘丘暴徒 21020201，不下发武器所以 AI 起不来、站着不动；血条按累计伤害对数衰减，测试期间不会被打死。
  - 每秒推一次当前 DPS，结束汇总用时/总伤害/平均 DPS/命中次数/单次最高/元素分布。
  - 相对插件版的修正：靶子归入伪 group 50505051，击杀判定与结束清场真正生效；结算只走一次（计时结束、打死靶子、挑战失败、玩家掉线都会收尾并归还场景 challenge 槽）；开测前复活并回满队伍、充满元素能量；去掉插件里逐次命中的 `[DPSDiag]` info 日志。
  - 伤害数值仍由客户端上报，星/月反应伤害并入其触发元素那一栏，无法单独分列。
- 圣言自明机 TxtItemName：日志显示登录从未下发 ReliquaryOfferDataNotify（原先 deferred until open，但客户端开 UI 不发 UseItem）。现改为登录即 sendDataNotify（压 tip 7013），装备小道具也会同步。远程已重启。请再重登一次验证。

2026-09-13
- 烈焰花/冰雾花熄灭后无交互键：
  - 根因：ElementFlora 初始禁用交互；水/冰（或火打冰雾花）命中后未解锁；开放世界 SetGadgetEnableInteract 按 config/group 查找常失败。
  - 源码：GatherInteractHelper.tryUnlockElementalFlora；EntityBaseGadget/OreMiningHelper 命中解锁；ScriptLib 回退当前实体；toProto 用 safeClientInteractEnabled。
  - 本地注入：_build_inject_flora.py + _patch_scriptlib_flora.py → LunaGC-7.0.0.jar；备份 bak-before-flora-reinject-*；已重启。

2026-09-12
- 圣遗物重塑 / ReliquaryDust（启圣之尘 105006）：
  - 新增重塑核心：`ReliquaryDustSystem` + Select/Confirm handlers，扣尘、生成候选词条、确认采用/维持原状。
  - 协议：C2S 7273 选条重塑；S2C 25355 候选 Notify（与圣言自明机共用）；S2C 7281 SelectRsp（回传请求 PacketHead）；C2S 21945/2210 确认。
  - 候选期间背包保持旧词条，确认后再写入新词条，避免对比页左右相同。
  - UnionCmd 转发父包 header；圣言自明机发 25355 时保留 Dust pending。
  - 注入脚本：`_build_inject_dust.py` → 本地 `LunaGC-7.0.0.jar` / 远程 `LunaGC-src-run.jar`。
  - 远程：已打入 `/www/wwwroot/LunaGC-src-run.jar`（备份前缀 `bak-before-dust-`），`start-lunagc-src.sh` 重启生效。
  - 已知：点确认重塑后仍常需退出提醒才能进对比页（SelectRsp 真实 opcode 待核实）；对比数据与扣尘已可用。
  - Git：`8cc38f3` 已推送 `origin/main`。
2026-09-09
- 队伍写错队安全修复（不碰 TeamManager.class）：
  - 新增 `TeamSetupHelper`，`HandlerSetUpAvatarTeamReq` 改为按正确 teamId 写入。
  - 远程已验证 TeamManager SHA 未变；备份前缀 `bak-before-team-setup-safe-`。
  - 此前 javassist 整类重写 TeamManager 会导致白屏，已回滚后改用本方案。
- 远程队伍编队再次写错队：改第 N 队会写到第 1 队。
  - 根因：`setupAvatarTeam` 用 `getTeams().get(team.size())`（恒为 1）；后续整换 TeamManager 时把 9/8 队伍修复覆盖掉了。
  - 处理：javassist 仅重写 `setupAvatarTeam` 为按 `teamId` 查找，并始终回 `SetUpAvatarTeamRsp`；已重启远程。备份前缀 `bak-before-team-setup-`。
  - 源码同步：`TeamManager.setupAvatarTeam`、后台队伍顺序、`curAvatarTeamId`、新增 `HandlerMoveTeamReq`/`PacketMoveTeamRsp`。

2026-09-08
- 已将菈乌玛一命回血 + 菈乌玛/奈芙尔基础精通修复同步到远程 `/www/wwwroot/LunaGC-src-run.jar`（15 个 class），备份前缀 `bak-before-lauma-nefer-`，并用 `start-lunagc-src.sh` 重启。
- 修复菈乌玛 / 奈芙尔缺少角色面板基础元素精通：
  - Excel 已有 `elementMastery`（菈乌玛 200、奈芙尔 100），但 `AvatarData` 未反序列化、`recalcStats` 未写入。
  - 现从角色表加载并在属性重算时设为 EM 底数（突破精通仍叠加）。
  - 已打入本地 `LunaGC-7.0.0.jar`（备份 `bak-before-base-em-`）并重启。
- 修复菈乌玛一命「生之纺线」回血不生效（二次加固）：
  - 根因：月绽放后客户端多半只推 MODIFIER_CHANGE，不走 ApplyModifier，首次 helper 从未触发。
  - E(11192)/Q(11195) 开 20s 窗口；C1 HealHP modifier / TriggerAbility / 月绽放 GV / 反应 mixin 均可结算治疗。
  - 已打入本地 `LunaGC-7.0.0.jar`（备份 `bak-before-lauma-c1-v2-`）并重启。
- 修复菈乌玛一命「生之纺线」回血不生效：
  - 新增 `LaumaC1HealHelper`：月绽放 ApplyModifier(HealHP) 时按精通×500% 治疗当前场上角色，ICD 1.9s。
  - `ActionHealHP` 把全局值编进公式，并支持 otherTargets=CurLocalAvatar。
  - `ActionApplyModifier` / 元素反应 mixin 改为同步执行 onAdded，避免 GetFightProperty→HealHP 竞态。
  - `ActionAttachModifier` 对 Team 目标执行 onAdded（挂 TeamHandler）。
  - 已打入本地 `LunaGC-7.0.0.jar`（备份前缀 `LunaGC-7.0.0.jar.bak-before-lauma-c1-`）。
- 修复陆地误播枫丹水面蓝色水圈：默认角色/队伍能力里误挂了潜水相关 embryo（Avatar_FluidAgitator、SceneAbility_DiveVolume、Ability_Avatar_Dive_Team 及一批 Dive Absorb）。登录/切队/传送/死亡会重建 ability，客户端当成进水播涟漪。DiveVolume 已由场景 levelElementAbilities 加载，无需挂在角色上。
  - 源码：`GameConstants.java` 已删掉上述默认项。
  - 远程：对 `/www/wwwroot/LunaGC-src-run.jar` 做 javassist 定点补丁（不清换本地 class），在 `GameConstants.<clinit>` 末尾过滤潜水相关字符串并重算 `DEFAULT_ABILITY_HASHES`；备份前缀 `LunaGC-src-run.jar.bak-before-fluid-agitator-`，已重启。
- 远程队伍管理修复（基于 `D:\7.0测试\LunaGC-7.0-team-manage-fix.zip`）：
  - 对当前远程 jar 做 javassist 定点补丁（非整包替换 TeamManager，避免再次误伤产球链路）。
  - 修复：编队确认转圈（正确 teamId + 始终回 SetUpAvatarTeamRsp）、登录不保留后台/扩队、移动队伍（HandlerMoveTeamReq）。
  - 已写入 `/www/wwwroot/LunaGC-src-run.jar` 并重启；备份前缀 `LunaGC-src-run.jar.bak-before-team-manage-`。
- 远程产球异常排查与恢复：
  - 原因：远程 jar 中 AbilityManager / TeamManager / PredicateEvaluator 等 7 个 class 被误换成与本地旧包一致的版本，通用 GENERATE_ELEM_BALL 产球链路异常，表现为多人（含菲林斯）不产球。
  - 处理：从备份 `LunaGC-src-run.jar.bak-before-columbina-ps2-20260908114228` 恢复这 7 个 class；保留菲林斯扣能修复（EnergyManager + EntityAvatar），并已重启远程。
- 修复哥伦比娅突破天赋「新月自己的法则」山月草露 UI：
  - 普通草露进场种子改为 0（原先种子=3 导致加不上、看不到变化）。
  - 新增 `RGV_TempMoonOvergrowPoint` 同步（山月草露独立 UI 计数，上限 3）。
  - 月绽放触发 `AddMoonOverGrowCount` 时由服务端推送该全局值。
  - 仅打入本地 LunaGC-7.0.0.jar 并重启本地（未动远程）。
- 修复哥伦比娅突破天赋「新月自己的法则」（PermanentSkill_2 / 山月草露）：
  - 停止将 MoonOvergrowPoint_All 强制写成 50/100（官方能力配置上限为 3），改为按客户端同步值记录。
  - ActionAddGlobalValue 仅在 useLimitRange=true 时钳制，修复 AddCount ICD 被 maxValue=0 清零。
  - PredicateEvaluator 支持 DynamicFloat 数组比较，并正确解析 ByUnlockTalentParam 目标。
- 修复菲林斯特殊元素爆发「雷霆交响」能量消耗异常：原先释放会清空当前全部能量，现按技能表 costElemVal 扣除（特殊爆发扣 30，普通爆发扣 80）。
- 同时修复同一段 Q 被多次通知导致重复扣费的问题（EvtDoSkillSucc + Ability 确认去重）。
- 已将修复打入本地 LunaGC-7.0.0.jar，并同步到远程 /www/wwwroot/LunaGC-src-run.jar（EnergyManager + EntityAvatar），远程已重启生效。
- 已提交并推送到 GitHub main：c49dd70 fix(Flins): 特殊元素爆发按 costElemVal 扣能量，避免清空整条能量条。
